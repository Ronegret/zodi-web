# Server-only. Do not expose this value in frontend code.
CLAUDE_API_KEY=replace_with_server_side_key

# Optional: set this in Hostinger/server environment instead of a file.
# If environment variables are not available, create ../.zodi-secret.php
# one level above public_html:
# <?php return ['CLAUDE_API_KEY' => 'replace_with_server_side_key'];

